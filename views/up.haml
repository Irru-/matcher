%html
	%body
		%h1 File uploader!
		%form(method="post" enctype='multipart/form-data')
			%input(type='file' name='myfile')
			%br
			%h3 Maximale afstand 	
			%input(type='text' name='match[di]')
			%br
			%input(type='submit' value='Upload!')